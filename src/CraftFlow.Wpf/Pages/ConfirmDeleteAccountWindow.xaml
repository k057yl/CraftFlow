<Window x:Class="CraftFlow.Wpf.Windows.ConfirmDeleteAccountWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Подтверждение удаления аккаунта" 
        Height="260" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="{DynamicResource AppBackgroundBrush}">
    <Border Padding="20">
        <StackPanel>
            <TextBlock Text="Удаление учетной записи" FontSize="18" FontWeight="Bold" Foreground="#DC2626" Margin="0,0,0,10"/>

            <TextBlock Text="Для подтверждения введите ваш Email:" Foreground="{DynamicResource AppForegroundBrush}" Margin="0,0,0,5"/>
            <TextBlock x:Name="RequiredEmailTextBlock" Text="user@example.com" FontWeight="Bold" Foreground="{DynamicResource AppForegroundBrush}" Margin="0,0,0,10"/>

            <TextBox x:Name="EmailInputTextBox" Height="32" VerticalContentAlignment="Center" Margin="0,0,0,20"/>

            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Button Content="Отмена" Click="Cancel_Click" Height="36"/>
                <Button Grid.Column="2" Content="Удалить навсегда" Click="Confirm_Click" Background="#DC2626" Foreground="White" FontWeight="Bold" Height="36"/>
            </Grid>
        </StackPanel>
    </Border>
</Window>