<Page x:Class="CraftFlow.Wpf.Pages.AuditPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="AuditPage">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="SYSTEM AUDIT LOGS" FontSize="20" FontWeight="Bold" Margin="0,0,0,15"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="REFRESH LOGS" Click="Refresh_Click" Height="28" Width="120" Margin="0,0,10,0"/>
            <Button Content="EXPORT CSV" Click="ExportCsv_Click" Height="28" Width="120"/>
        </StackPanel>

        <DataGrid Grid.Row="2" x:Name="AuditDataGrid" AutoGenerateColumns="True" IsReadOnly="True" Background="White"/>

        <TextBlock Grid.Row="3" x:Name="StatusTextBlock" Text="READY" Foreground="Blue" FontWeight="Bold" Margin="0,10,0,0"/>
    </Grid>
</Page>